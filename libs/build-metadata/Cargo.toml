[package]
name = "vanling_build_metadata"
edition.workspace = true
version.workspace = true


build = "build.rs"

[lib]
path = "src/lib.rs"
crate-type = [
  "rlib",
  # "dylib",
]

[features]


[dependencies]

[build-dependencies]
built = { workspace = true }

[lints]
