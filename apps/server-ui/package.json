{
  "name": "@chobits/server-ui",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --project tsconfig.app.json",
    "preview": "vite preview"
  }
}
