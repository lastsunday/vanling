{
  "name": "@vanling/server-ui",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --project tsconfig.app.json",
    "preview": "vite preview"
  },
  "dependencies": {
    "recharts": "^3.10.1"
  }
}
